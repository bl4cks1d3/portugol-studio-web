
{
  "entities": {
    "Example": {
      "title": "Example",
      "description": "A Portugol code example",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "category": { "type": "string" },
        "code": { "type": "string" },
        "description": { "type": "string" },
        "order": { "type": "number" }
      },
      "required": ["title", "category", "code"]
    },
    "Question": {
      "title": "Question",
      "description": "A programming challenge for students",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "initialCode": { "type": "string" },
        "expectedOutput": { "type": "string" },
        "difficulty": { "type": "string", "enum": ["easy", "medium", "hard"] }
      },
      "required": ["title", "description", "initialCode"]
    },
    "SharedCode": {
      "title": "SharedCode",
      "description": "Portuguol code shared via a link",
      "type": "object",
      "properties": {
        "code": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["code", "createdAt"]
    }
  },
  "firestore": {
    "/shared_codes/{codeId}": {
      "schema": "SharedCode",
      "description": "Stored code for shortened share links"
    },
    "/examples/{exampleId}": {
      "schema": "Example",
      "description": "Collection of Portugol examples"
    },
    "/questions/{questionId}": {
      "schema": "Question",
      "description": "Collection of programming questions"
    }
  }
}
